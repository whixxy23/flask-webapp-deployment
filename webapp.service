[Unit]
Description=Module 3 Web App
After=network.target

[Service]
User=deploy
WorkingDirectory=/home/deploy/webapp
EnvironmentFile=/etc/webapp.env
ExecStart=/home/deploy/webapp/venv/bin/gunicorn -b 127.0.0.1:5000 app:app
Restart=always

[Install]
WantedBy=multi-user.target
